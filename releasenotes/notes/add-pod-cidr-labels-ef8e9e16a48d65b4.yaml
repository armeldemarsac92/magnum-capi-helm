---
features:
  - |
    Added support for ``kube_pods_cidr`` and ``kube_services_cidr`` cluster
    template labels that allow operators to override default Kubernetes
    network CIDR ranges at the template level. This resolves IP overlap
    issues where default pod CIDRs conflict with existing corporate networks,
    eliminating the need for manual post-deployment Calico configuration
    changes.
